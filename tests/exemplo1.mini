// Teste abrangente da Mini-Lang
/* isso é um comentári

o
*/



var x: int = 10;
var y: int = 20;

var flag: bool = true;

def somar(a: int, b: int): int {
    return a + b;
}

var x : int = 30; 
set flag = 10; 
 

if (x < y) {
    print "x";
} else {
    set x = somar(x, 5);
}

while (x > 0) {
    set x = x - 1;
    if (x == 5) {
        print "Metade";
    }
}

/*Apenas para teste do Analisador semantico*/
var es: int = 10; 
var teste: bool = true; 
var soma: int = 0; 

set soma = es + teste; 

def soma(a: int, a: int): int{
    var local: int = 0; 
    set local = soma(a, b); 
}

set soma = es + local; 